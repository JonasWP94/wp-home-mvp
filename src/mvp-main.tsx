import React from 'react'
import ReactDOM from 'react-dom/client'
import MvpWizard from './components/MvpWizard'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MvpWizard />
  </React.StrictMode>,
)
