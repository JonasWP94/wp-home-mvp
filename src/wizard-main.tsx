import React from 'react'
import ReactDOM from 'react-dom/client'
import WizardNew from './components/WizardNew/index'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <WizardNew />
  </React.StrictMode>,
)
